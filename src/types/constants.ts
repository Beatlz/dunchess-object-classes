import { ColorType } from ".."
import { PieceNameType } from ".."

// Game
export const COLORS: { [key: string]: ColorType } = { LIGHT: `LIGHT`, DARK: `DARK` }

// PIECE
// Names
export const PIECE_ROOK: PieceNameType = `ROOK`
export const PIECE_KNIGHT: PieceNameType = `KNIGHT`
export const PIECE_BISHOP: PieceNameType = `BISHOP`
export const PIECE_QUEEN: PieceNameType = `QUEEN`
export const PIECE_KING: PieceNameType = `KING`
export const PIECE_PAWN: PieceNameType = `PAWN`
// Initiation
export const INIT_LIVES = 1
